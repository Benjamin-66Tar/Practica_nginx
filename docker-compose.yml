services:
  nginx:
    image: nginx:alpine
    container_name: nginx-practice
    ports:
      - "80:80"
      - "8080:8080"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./data:/data:ro
    restart: unless-stopped
